## 프로세스
* 타이머 인터럽트 (타임아웃 인터럽트): 프로세스 CPU 사용시간 제한
* 문맥: 프로세스 수행 재개를 위해 기억해야하는 정보 (PCB 저장)
* 문맥 교환: 기존 프로세스의 문맥은 PCB 백업, PCB에서 문맥을 복구하여 새로운 프로세스 실행

### 포그라운드 프로세스
* 사용자가 보는 공간에서 실행

### 백그라운드 프로세스 (daemon)
* 사용자가 보지 못하는 곳에서 실행

### 프로세스 메모리 정보
* 커널 영역
  * 프로세스 제어 블록 (PCB)
    * 프로세스의 ID, 레지스터, 상태, CPU 스케줄링 정보, 메모리 정보, 입출력장치 관련 정보
    * PCB 경우 프로세스 테이블 형태로 관리
* 사용자 영역
  * 코드 영역(정적): 실행 가능한 명령어 저장 공간, 읽기 전용 공간
  * 데이터 영역(정적): 프로그램 실행되는 동안 유지할 데이터 저장 공간
  * 힙 영역(동적): 프로그램 개발자가 직접 할당 공간 (반환 필요)
  * 스택 영역(동적): 일시적으로 사용하는 데이터 저장 공간
    * 스택 트레이스: 특정 시점에 스택 영역에 저장된 함수 호출 정보

### 프로세스 상태
* 생성: 프로세스가 메모리에 적재되어 PCB 할당받은 상태
* 준비: 실행 차례를 기다리고 있는 상태
  * 디스패치: 준비 상태 프로세스가 실행 상태로 전환
* 실행: CPU 할당 받아 실행 상태
* 대기: 입출력 작업 요청 및 대기 상태 입출력 작업이 완료되면 준비 상태 전환
  * 블로킹 입출력: 입출력 작업을 위해 실행 막힘
  * 논블로킹 입출력: 입출력 작업을 위해 실행이 안막힘
* 종료: 프로세스가 종료되어 PCB, 프로세스 사용 메모리 정리

### 멀티프로세스
* 기본적으로 서로 다른 프로세스들은 자원을 공유하지 않는다.
* PID 값이 다르고 자원을 독립적으로 할당

### 멀티스레드
* 같은 프로세스에서 여러 스레드들은 프로세스 자원을 공유
* PID, 여러 자원 공유
* 스레드 ID, 프로그램 카운터, 레지스터 값, 스택은 다르게 구성

### 프로세스 통신 (IPC)
* 공유 메모리: 프로세스가 공유할 수 있는 메모리 영역을 두는 방식
* 메시지 전달: 커널을 통해 프로세스 사이에 데이터 공유 방식
  * 파이프 (단방향)
  * 시그널
  * 소켓
  * 원격 프로시저 호출 (RPC)

## 동기화와 교착 상태

### 임계구역
* 공유 자원에 접근하는 코드 중 동시에 실행했을 때 문제가 발생할 수 있는 코드

### 레이스컨디션
* 프로세스 혹은 스레드가 동시에 임계 구역의 코드를 실행하여 발생한 문제
* 레이스컨디션 방지 방법
  * 실행 순서 제어
  * 상호 배제

### 조건 변수, 모니터 (실행 순서 제어)
* 조건변수: 특정 조건에 따라 실행 순서 제어를 위한 동기화 도구
  * wait(): 호출한 프로세스 및 스레드를 대기 상태 전환
  * signal(): 대기 상태 프로세스 및 스레드 실행 재개
* 모니터: 공유 자원과 공유 자원을 다루는 인터페이스로 구성된 동기화 도구
  * 상호 배제와 실행 순서 제어를 위한 동기화 도구
  * 모니터 진입을 위해 큐를 통해 대기 필요
* 자바 `synchronized`

### 뮤텍스 락 (상호 배제)
* 동시에 접근해서는 안 되는 자원에 상호 배제를 보장하는 동기화 도구
* 하나의 공유 자원을 고려하는 동기화 도구
* 뮤덱스 락의 원리
  * 임계 구역 접근을 위해 락을 획득
  * 임계 구역에서 작업이 끝났다면 락을 해제
* 자바 `lock`

### 세마포어 (상호 배제)
* 공유 자원이 여러개 있는 상황에서 동기화 도구
* 공유 자원 사용 가능한 갯수를 변수로 관리
  * wait(): -1
  * signal(): +1
* 자바 `Semaphore`

### 교착 상태
* 일어나지 않을 사건을 기다리며 프로세스의 진행이 멈춰 버리는 현상
* **교착 상태 예방**
  * 상호 배제: 특정 자원을 여러 프로세스가 사용 못하는 경우
  * 점유와 대기: 자원을 점유한 상태에서 다른 자원을 기다리는 경우
  * 비선점: 해당 자원을 이용하기 위해 작업이 끝나는 경우에만 사용하는 경우
  * 원형 대기: 여러 프로세스의 상태가 원의 형태를 이루는 경우

### 교착 상태 해결
* 교착 상태 예방: 교착 상태 발생하지 않도록 자원 분배 방식을 통해 예방
* 교착 상태 회피: 자원을 조금씩 할당, 교착 상태 위험이 있는 경우 자원 할당을 않하는 방식
* 검출 후 회복: 교착 상태 발생 여부 확인 후 조치

## CPU 스케줄링
### 우선순위
* 운영체제는 프로세스별 우선순위 PCB 명시
* 사용자가 일부 프로세스의 우선순위를 높일 수도 있음
* `ps` 명령어
  * PRI: 실제 우선순위
  * NI: 사용자 설정 우선순위

### CPU 활용률
* 전체 CPU 가동 시간 중 구동되는 시간의 비율
* CPU 활용률 위해 입출력 작업이 많은 프로세스의 우선순위를 높게 유지
  * 입출력 프로세스의 경우 대기 상태가 많기 때문에

### 스케줄링 큐 
* 특정 자원을 이용하고 싶은 프로세스 PCB 대기 목적
* FIFO 및 PCB 우선순위 고려
* 준비큐: 실행이 준비된 PCB 큐
* 대기큐: 다른 자원(입출력장치) 결과를 기다리는 PCB 큐

### 선점형 스케줄링 
* 대기 상태로 전환, 준비 상태로 전환
* 자원 사용 시간 제한
* 장점: CPU 독점을 막고 골고로 CPU 자원 배분
* 단점: 문맥 교환 오버헤드 발생

### 비선점형 스케줄링
* 대기 상태로 전환
* 별도 자원 사용 시간 제한 없다.
* 장점: 비교적 문맥 교환 오버헤드 적다.
* 단점: 프로세스 대기 시간 증가

### CPU 스케줄링 알고리즘
* 선입 선처리 스케줄링: 단순히 큐에 삽입된 순서대로 CPU 자원을 할당
  * 호위 효과 발생 (나중에 삽입된 프로세스 실행 지연 문제)
* 최단 작업 우선 스케줄링: 큐에 삽입된 프로세스 중 CPU 이용 시간이 가장 짧은 순서로 자원 할당
* 라운드 로빈 스케줄링: 선입선처리 + 타임 슬라이스(시간 제한) 정책 자원 할당
* 최소 잔여 시간 우선 스케줄링: 최단 작업 우선 스케줄링 + 라운드 로빈 스케줄링
* 우선순위 스케줄링: 프로세스의 우선순위 기준으로 CPU 자원 할당
  * 아사 현상 발생 (우선순위가 낮은 경우 오랫동안 대기)
  * 에이징을 통해 해결 (오랫동안 대기한 프로세스의 우선순위를 높이는 방식)
* 다단계 큐 스케줄링: 우선순위별로 여러 개의 준비 큐를 통해 CPU 자원 할당
* 다단계 피드백 큐 스케줄링: 다단계 큐 스케줄링 + 큐 사이 이동 기능 추가 CPU 자원 할당

### 리눅스 CPU 스케줄링
* CFS: 프로세스에 대한 실제 실행 시간이 아닌, 프로세스 가중치 고려한 실행 시간 스케줄링
* `/proc/<PID>/sched` 파일 확인 가능
  * se.vruntime
  * se.load.weight

## 가상화 메모리
* 실제 메모리보다 큰 프로세스를 실행
* 실행하고자 하는 프로그램 일부만 메모리에 적재

### 물리 주소
* 메모리의 하드웨어 상 살제 주소

### 논리 주소 (가상 주소 공간)
* 프로세스마다 부여되는 논리적 주소

### 메모리 관리 장치 (MMU)
* CPU 메모리 사이에 논리 주소를 물리 주소로 변환
* 페이지 테이블을 참조하여 물리 주소 전달

### 가상 메모리 관리 기법
* 페이징
  * 논리 주소 공간을 페이지라는 일정한 단위, 물리 주소 공간을 프레임 단위 관리
  * 페이지 테이블을 통해 페이지와 프레임 위치 변환
    * 유효 비트: 해당 페이지 메모리, 보조 기억장치 적재 여부 표시 (페이지 폴트 발생)
    * 보호 비트: 페이지 보호 기능 (읽기, 쓰기, 실행 접근 권한)
    * 참조 비트: 페이지 접근 여부 표시
    * 수정 비트: 수정 여부 표시
  * 내부 단편화 발생 
* 세그멘테이션
  * 가변적인 세그먼트 단위 관리
  * 외부 단편화 발생

### 단편화
* 외부 단편화: 연속 메모리 할당 혹은 세그멘테이션 경우 프로세스 메모리 밖 미사용 공간 발생
  * 추가적으로 물리 메모리보다 큰 프로세스 실행 불가
* 내부 단편화: 페이징 메모리 할당 경우 프로세스 메모리 안 미사용 공간 발생

### 스와핑
* 오랫동안 사용되지 않은 프로세스를 보조 기억장치를 통해 관리
* 스왑 아웃: 메모리 -> 보조기억장치
* 스왑 인: 보조기억장치 -> 메모리

### 페이징 교체 알고리즘
* 요구 페이징: 메모리에 필요한 페이지만 적재하는 기법
* 순수 요구 페이징: 아무런 페이지 없이 무작정 프로세스 실행
* 페이지 교체 알고리즘: 보조기억장치에서 가져오거나 내보낼 페이지 교체 알고리즘
  * FIFO: 먼저 적재된 순서로 스왑 아웃
  * 최적: 앞으로 사용 빈도가 낮은 페이지 에측 교체
  * LRU: 가장 적게 사용한 페이지 교체

### 페이지 폴드 종류
* 메이저 페이지 폴트: 보조기억장치를 통한 페에지 폴트
* 마이너 페이지 폴트: 페이지 테이블 상에 반영 단계의 페이지 폴트

## 파일 시스템

### 파일 구성 
* 파일 이름
* 파일 실행 정보
* 파일 메타데이터

### 파일 디스크립터
* 프로세스가 파일을 구별하기 위한 식별 정보
* 파일 뿐만 아니라 입출력 장치, 소켓 항목도 식별

### 디렉터리
* 여러 파일들의 일목요연한 관리
* 트리 구조 디렉토리

### 디렉터리 엔트리
* 테이블 형태로 표현된 정보
* 파일 이름, 파일 위치 유추 정보 (파일 아이노드)

### 블록
* 파일과 디렉토리를 읽고 쓰는 단위
* 보통 4096Byte

### 블록 저장 방식
* 연결 할당: 각 블록의 일부에 다음 블록의 주소를 저장
* 색인 할당: 특정 블록에 관련 모든 블록 주소 명시

### 파일 시스템
* 포매팅: 파일 시스템을 설정 (FAT32)
* 파티션 구분별로 포매팅 가능

### 아이노드
* 파일 이름 제외한 파일의 거의 모든 정보 저장소
* EXT, EXT2, EXT3, EXT4 아이노드 기반 파일 시스템
* 하드 링크 파일: 원본 파일에 아이노드 공유
* 심볼릭 링크 파일: 원보 파일과 별도 아이노드 생성

### 마운트
* 다른 파일 시스템을 편입 시키는 작업
