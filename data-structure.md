# 자료구조
* 데이터를 효울적으로 저장하고 관리하는 방법

### 배열
* 일정한 메모리 공간을 차지하는 요소들이 순차적으로 나열된 자료구조
  * 정적 배열: 프로그램 실행 전 크기 조정 배열 (자바 `Array`)
  * 동적 배열: 프로그램 실행 과정 크기 조정 배열 (자바 `ArrayList`)
* 각 요소에 인덱스 매겨짐
* 데이터 접근 시간이 일정
* 인덱스를 활용한 검색 연산의 경우 O(1)
* 추가, 삭제 연산의 경우 O(n)

### 연결 리스트 
* 노드의 모음으로 구성된 자료구조 (자바 `LinkedList`)
  * 환형 연결 리스트: 단방향, 꼬리 노드 -> 헤드 노드 구성
  * 이중 환형 연결 리스트: 양방향, 꼬리 노드 -> 헤드 노드, 헤드 노드 -> 꼬리 노드 구성
* 노드들이 한 쪽 방향으로 꼬리에 꼬리를 무는 형태
* 모든 노드는 반드시 메모리 내에 순차적으로 저장되어 있을 필요 없다.
* 첫번째 노드: 머리, 마지막 노드: 꼬리
* 검색 연산의 경우 O(n)
* 추가, 삭제 연산의 경우 O(1)

### 스택 (LIFO)
* 한 쪽에서만 데이터의 삽입 및 삭제 가능한 자료구조 (자바 `Stack`)
* 함수 호출, 뒤로가기, 미로 이동의 상황에서 활용 가능 
  * psuh: 스택에 데이터 삽입
  * pop: 스택 데이터 추출

### 큐 (FIFO)
* 한 쪽으로 데이터를 삽입, 다른 한쪽으로 데이터를 삭제 가능한 자료구조
  * 인큐: 데이터 삽입 연산 (자바 `Queue`)
  * 디큐: 데이터를 삭제 연산 (자바 `Deque`)
* 다양한 큐의 형태
  * 원형 큐: 양쪽으로 연결된 원형 형태 큐
  * 양방향 큐 (덱): 양쪽으로 데이터 삽입, 삭제 가능한 큐
  * 우선순위 큐: 우선순위가 높은 순서로 처리되는 큐
    * 힙 자료구조 기반 구현

### 해시 테이블
* 키와 값 대응으로 이루어진 표와 같은 자료구조 (자바 `HashMap`)
* 키를 통해 데이터를 버킷에 저장
* 해시 함수를 통해 키 인자를 버킷에 접근할 인덱스로 반환
* 검색, 삽입, 삭제, 연산의 경우 O(1)
* 상대적으로 많은 메모리 공간 소모

### 해시 충돌
* 서로 다른 키에 같은 해시 값이 대응되는 상황
* 해시 충돌 해결 방법
  * 체이닝: 충돌이 발생한 인덱스에 데이터를 연결 리스트 추가 (성능 저하)
  * 개방 주소법: 다른 인덱스에 데이터 저장
    * 선형 조사법: 충돌 이후 인덱스에 순차적으로 추가 (데이터 군집화)
    * 이차 조사법: 충돌이 발생한 인덱스 제곱수 인덱스에 데이터 추가
  * 이중 해싱: 2개의 해시 함수 사용 (성능 저하)

### 해시 함수
* 임의 길이 데이터를 고정된 길이의 데이터로 변환하는 단방향 함수
* 무작위 값, 단방향 암호, 무결성 검증 활용
* 해시 알고리즘: MD5, SHA-1, SHA-256, SHA-512, SHA3, HMAC 등

### 트리
* 주로 계층적 구조를 표현하는 자료구조
* 노드는 상하 관계 형성, 간선(링크) 통해 구성
* 사이클을 형성하지 않고 연결된 노드 구성
* 차수 (degree): 각 노드가 갖는 자식 노드 수
* 깊이 (level): 루트 노드에서 시작해 특정 노드 이르기까지 거치는 간선 수
* 서브 트리: 트리 안에 포함되는 있는 트리

### 트리 종류
* 이진 트리: 자식 노드의 개수 2개 이하 트리
* 편향된 이진 트리: 모든 자식 노드가 한 쪽으로 치우친 이진 트리
* 정 이진 트리: 모든 지식 노드 개수가 0 또는 2개인 이진 트리
* 포화 이진 트리: 모든 지식 노드 개수가 2개인 이진 트리
* 완전 이진 트리: 마지막 레벨을 제외한 모든 레벨이 2개의 자식 노드 구성 그리고 마지막 레벨의 경우 왼쪽 부터 노드 존재
* 이진 탐색 트리: 왼쪽 서브 트리는 해당 노드보다 작은값 오른쪽 서브 트리는 해당 노드보다 큰값
* 최대-힙: 부모 노드가 자식 노드 값보다 큰 값으로 구성된 이진 트리
* 최소-힙: 부모 노드가 자식 노드 값보다 작은 값으로 구성된 이진 트리
* 자가 균형 이진 탐색 트리: 편향된 형태의 트리 방지 목적
  * AVL 트리: AVL 트리는 노드가 추가되거나 삭제될 때마다 트리 전체의 균형을 체크 (BF)
  * RB 트리: 색상 지정 정책 및 트리 회전 통해 트리에 균형을 유지한다.
* B 트리: 한 노드에 여러 자식 노드를 가질 수 있는 탐색 트리 (다진 탬색 트리)
  * 오름차순으로 키값, 하위 노드의 주소 존재
* B+ 트리: 실질적 데이터가 모두 최하위 리프 노드 위치

### 노드 종류
* 부모 노드: 기준 노드보다 상위 위치한 노드
* 자식 노드: 기준 노드보다 하위 위치한 노드
* 형제 노드: 같은 부모 노드 공유
* 루트 노드: 부모 노드가 없는 최상단 노드
* 리프 노드: 더 이상의 자식 노드가 없는 최하단 노드

### 트리의 순회
* 트리의 모든 노드를 한 번씩 방문하는 방식
* 전위 순회: 루트 노드 -> 왼쪽 서브트리 -> 오른쪽 서브트리
* 중위 순회: 왼쪽 서브트리 -> 루트 노드 -> 오른쪽 서브트리
* 후위 순회: 왼쪽 서브트리 -> 오른쪽 서브트리 -> 루트 노드

### 그래프
* 정점과 간선(링크) 연결 형태 자료 구조
* 연결 관계 표현 자료구조
* 트리의 경우에도 그래프 종류 중 하나

### 그래프 종류
* 연결 그래프: 정점 사이 경로가 존재
* 비연결 그래프: 정점 사이 경로가 미존재
* 방향 그래프: 간선에 방향이 있는 그래프
* 무방향 (양방향) 그래프: 간선에 방향이 없는 그래프
* 가중치 (비용) 그래프: 간선에 가중치가 부여된 그래프
* 서브 그래프: 그래프의 부분 그래프

### 그래프 구현 방법
* 인접 행렬: 이차원 배열 기반 구현
  * N 정점의 개수 경우
  * N * N 행렬 정점 연결 여부 혹은 가중치 표현
* 인접 리스트: 연결 리스트 기반 구현
  * 연결된 노드를 연결 리스트를 통해 연결

### 깊이 우선 탐색 (DFS)
* 그래프 더 이상 방문 가능한 정점이 없는 경우까지 최대한 깊이 탐색
* 배열을 통해 방문 여부 확인
* 스택을 통해 정점 방문 도중 뒤로가기

### 너비 우선 탐색 (BFS)
* 최대한 넓게 탐색하는 탐색 방법
* 연결되어 있는 노드를 최대한 접근
* 배열을 통해 방문 여부 확인
* 큐를 통해 연결된 정점을 저장
