# 다자인 패턴

## 생성
* 객체를 생성하는 과정을 캡슐화하여 객체 생성과 사용을 분리

### Singleton
* 객체를 하나만 생성하여 전역에서 공유하는 패턴

### Factory Method
* 객체 생성 책임을 하위 클래스 또는 팩토리에 위임하는 패턴

### Abstract Factory
* 관련된 객체들을 묶어서 생성하는 패턴

### Builder
* 복잡한 객체를 단계적으로 생성하는 패턴

### Prototype
* 기존 객체를 복제하여 새로운 객체를 생성하는 패턴

## 구조
* 객체와 클래스를 조합하여 더 큰 구조 구성

### Adapter
* 호환되지 않는 인터페이스를 연결하는 패턴

### Bridge
* 추상화와 구현을 분리하여 독립적으로 확장하는 패턴

### Composite
* 개별 객체와 집합 객체를 동일하게 다루는 패턴

### Decorator
* 객체에 기능을 동적으로 추가하는 패턴

### Facade
* 복잡한 시스템을 단순한 인터페이스로 감싸는 패턴

### Flyweight
* 객체를 공유하여 메모리 사용을 줄이는 패턴

### Proxy
* 실제 객체 대신 대리 객체를 통해 접근을 제어하는 패턴

## 행위
* 객체 간 책임 분배와 통신 방식을 정의

### Strategy
* 알고리즘을 교체할 수 있는 패턴

### Observer
* 상태 변경을 구독자에게 알리는 패턴

### Template Method
* 알고리즘의 뼈대를 정의하는 패턴

### Command
* 요청을 객체로 캡슐화하는 패턴

### State
* 상태에 따라 행동을 변경하는 패턴

### Iterator
* 컬렉션을 순차적으로 탐색하는 패턴

### Chain of Responsibility
* 요청을 여러 객체가 순차적으로 처리하는 패턴

### Mediator
* 객체들이 직접 통신하지 않고 중재자를 통해 통신하는 패턴

### Memento
* 객체 상태를 저장했다가 나중에 복원하는 패턴

### Visitor
* 객체 구조는 수정하지 않고 새로운 기능을 추가하는 패턴

### Interpreter
* 정해진 문법을 해석하는 패턴