# 컴퓨터 구조

## 데이터
* 비트: 0과 1을 나타내는 가장 작은 정보의 단위
* 바이트: 여덟 비트를 묶은 단위 (kB -> MB -> GB -> TB)
* 워드: CPU 한 번에 처리할 수 있는 데이터 크기 (32비트, 64비트)  

**2진법으로 표현하는 경우 숫자의 길이가 길어지는 문제가 있어 16진법 병행 사용**

### 부동 소수점
* 소수점 위치가 고정되지 않은 실수 표현 방식
* 일반적으로 가수부와 지수부로 구성
* 컴퓨터에서 소수점 처리를 위해 **IEEE 754** 방식 사용 (부호 + 지수 + 가수)
  * 1.XXX 해당 형태로 처리되므로 가수부의 경우 소수 부분만 저장
  * 지수부의 경우 바이어스 값이 더해져서 저장 (지수 표현 8비트의 경우 2^7-1 127)
  * 무한소수 표현시 제한된 컴퓨터의 저장공간으로 일부 소수점을 생략하여 처리
```java
public class FloatingPointExample {
    public static void main(String[] args) {

        double a = 0.1;
        double b = 0.2;
        
        if (a + b == 0.3) {
            System.out.println("같다");
        } else {
            System.out.println("다르다");
        }
    }
}
```

### 문자
* 문자 집합: 컴퓨터가 이해할 수 있는 문자들의 집합 (ASCII, EUC-KR, Unicode)
* 문자 인코딩: 문자 집합에 속한 문자를 컴퓨터가 이해할 수 있는 0, 1 형태로 변환하는 과정 (A → 인코딩 → 01000001)
* 문자 디코딩: 0, 1 형태의 문자 코드를 사람이 이해할 수 있는 문자로 변환하는 과정 (01000001 → 디코딩 → A)

### ASCII
* 초장기 컴퓨터에서 사용하던 문자 집합
* 8비트 중 1비트는 패리티비트 사용
* `'0' -> 48` `'A' -> 65` `'a' -> 97`

### EUC-KR
* 아스키 코드에서 불가능한 한글 표현 목적

### Unicode
* 다양한 언어를 표현할 수 있는 문자 집합
* utf-8, utf-16, utf-32 다양한 인코딩 방식 존재

### base64
* 바이너리 데이터를 텍스트 형태로 변환하는 인코딩 방식
* 의도하지 않은 데이터 변경 방지
* 6비트 단위로 나누어서 표현 (부족한 자리의 경우 0 값으로 채움)

## 명령어
* 수행하는 동작(연산 코드), 수행하는 대상(오퍼랜드) 구성
* 기계어: CPU 이해할 수 있는 0, 1 표현된 언어

**CISC 기반 CPU 어셈플리어, RISC 기반 CPU 어셈플리어 별도 존재**

### CISC (x86, x64)
* 복잡하고 다양한 명령어 세트를 가진 컴퓨터
* 장점: 복잡한 작업을 명령어 한 줄로 끝낼 수 있어 소프트웨어 개발이 상대적으로 편리
* 단점: 하드웨어가 복잡해져 전력 소비가 크고 발열 발생

### RISC (ARM)
* 단순하고 축소된 명령어 세트를 가진 컴퓨터
* 장점: 하드웨어 구조가 단순하여 전력 소모가 적고 발열이 적습니다.
* 단점: 단순한 명령어들을 조합해서 복잡한 작업을 해야 하므로 메모리를 더 많이 사용

### 명령어 사이클
* CPU 메모리에 있는 명령으로 처리하는 과정
* 인출 사이클: 메모리에 있는 명령어를 CPU 가져오는 단계
* 실행 사이클: CPU 가져온 데이터를 실행하는 단계
* 간접 사이클: 명령어 실행을 위해 추가적으로 메모리에 접근하는 단계
* 인터럽트 사이클: 인터럽트 발생 시 현재 작업을 잠시 중단하고 인터럽트를 처리하는 단계

## CPU
* 클럭: 컴퓨터 부품 움직일 수 있게 하는 시간 단위 (Hz)
* 코어: CPU 명령어를 읽기, 해석, 실행하는 부품
* 스레드: 하나의 코어가 동시에 처리하는 명령어 단위 (병렬성)

### 연산장치 (ALU)
* 산술 연산과 논리 연산을 수행하는 장치

### 제어장치 (CU)
* CPU 내부에서 명령어 해석과 실행 흐름을 제어하는 장치

### 저장장치 (Register)
* CPU 안에 있는 작은 임시 저장장치
* WinDbg, gdb 같은 도구를 통해 디버깅
1. 프로그램 카운터 (PC)
   * 메모리에서 다음으로 읽어 들일 명령어 주소 저장
2. 명령어 레지스터
   * 메모리에서 방금 읽어 들인 명령어 저장
3. 범용 레지스터
   * 일반적인 상황에서 자유롭게 사용하는 레지스터
4. 플래그 레지스터
   * 연산의 결과 혹은 CPU 상태에 대한 부가 정보 값을 저장
   * 부호 플래그, 제로 플래그, 캐리 플래그, 오버플로우 플래그, 인터럽트 플래그, 슈퍼바이저 플래그
5. 스택 포인터
   * 메모리 내 스택 영역의 최상단 위치를 저장하는 레지스터

## 인터럽트
* CPU 작업을 방해하는 신호

### 인터럽트 서비스 루틴 (ISR)
* 인터럽트가 처리하기 위한 프로그램
* 인터럽트 핸들러라고도 불린다.

### 인터럽트 백터
* 인터럽트 서비스 루틴을 식별하기 위한 정보
* 인터럽트 서비스 루틴의 시작 주소 값 포함

### 동기 인터럽트 (예외)
* CPU 의해 발생하는 인터럽트
* 동기 인터럽트 종류
  * 폴트: 인터럽트가 발생한 명령어부터 실행
  * 트랩: 인터럽트가 발생한 명령어의 다음 명령어부터 실행
  * 중단: 프로그램을 강제로 중단시킬 수밖에 없는 인터럽트
  * 소프트웨어 인터럽트: 시스템콜에 의해 발생하는 인터럽트

### 비동기 인터럽트 (하드웨어 인터럽트)
* 주로 입출력장치에 의해 발생하는 인터럽트
* 입출력장치의 알림과 같은 역할
* 비동기 인터럽트 처리 순서
  1. 입출력장치는 인터럽트 신호 보냄
  2. CPU 실행 사이클 이후 명령어 인출전 인터럽트 여부 확인
  3. 인터럽트 요청 확인 후 인터럽트 플래그 통해 처리 가능 여부 확인
  4. 인터럽트 처리가 가능한 경우 CPU 현재 작업은 백업
  5. 인터럽트 서비스 루틴 실행
  6. CPU 백업 작업 복구 및 실행 재개
* 막을 수 없는 비동기 인터럽트(NMI) 존재
  * 하드웨어 고장 및 정전

### 명령어 병렬 처리 기법
* 여러 명령어 동시에 처리하여 성능을 높이는 기법

### 명령어 파이프라이닝
* 대표적인 명령어 병렬 처리 기법
* 명령어 인출, 해석, 실행, 결과 저장 과정을 겹치지만 않는다면 동시에 시행 기법

### 파이프라인 위험
1. 데이터 위험: 명령어 간의 의존성에 의해 발생
2. 제어 위험: 프로그램 카운터에 대한 갑작스러운 변화에 의해 발생
3. 구조적 위험: 다른 명령어가 같은 자원을 사용에 의해 발생

## 메모리

### RAM (임의 접근 메모리)
* CPU 실행할 대상을 저장
* 휘발성

### DRAM (Dynamic)
* 전력이 공급되는 경우에도 데이터가 동적으로 변한다.
* 데이터 소멸을 막기 위해 일정 주기로 데이터를 재활성화
* 소비 전력이 낮고 저렴하여, 집적도가 높아 메모리르 대용량 설계 용이

### SRAM (Static)
* 전력이 공급되는 경우에 데이터가 변하지 않는다.
* 속도가 빠르지만 소비 전력이 크고 가격이 비싸다.
* 집적도가 낮다.

### SDRAM (Synchronous Dynamic)
* DRAM 발전 형태
* 데이터 송수신에 유리
* CPU 클럭 타이밍에 맞춰 정보를 주고 받을수 있음

### DDR SDRAM (Double Data Rate SDRAM)
* 대역폭을 넓혀서 속도를 빠르게 만든 SDRAM

### 빅 엔디안과 리틀 엔디안
* 워드 단위에 CPU 데이터를 저장하는 방식
* MSB: 숫자의 크기에 가장 큰 영향을 미치는 숫자
* LSB: 숫자의 크기에 가장 적은 영향을 미치는 숫자
* **빅 엔디안**
  * 큰수를 낮은 번지부터 차레로 저장
  * 일상적으로 숫자 체계를 읽는 방식과 동일
* **리틀 엔디안**
  * 큰수를 큰 번지부터 차례로 저장
  * 수치 계산이 편리하다는 장점

## 캐시 메모리 (SRAM)
* CPU 연산 속도와 메모리 접근 속도의 차이를 줄이기 위해 탄생
* L1, L2, L3 여러 레벨에 캐시 메모리 존재
* 멀티 코어 프로세스의 경우 L1, L2 코어마다 존재
* L1 분리하는 경우 존재
  * L1I: 명령어 전용
  * L1D: 데이터 전용

### 캐시 히트
* 캐시 메모리가 예측한 데이터가 CPU 의해 실제로 사용되는 경우

### 캐시 미스
* 캐시 메모리에 데이터가 없어 CPU 직접 메모리에서 데이터를 가져오는 경우

**캐시 적중률: 캐시 히트 횟수 / (캐시 히트 횟수 + 캐시 미스 횟수)**

### 참조 지역성의 원리
* 특정 원칙에 따라 캐시 메모리 데이터 저장
* 시간 지역성: 최근 접근했던 메모리에 다시 접근하려는 경향 (변수)
* 공간 지역성: 근처 접근했던 메모리에 다시 접근하려는 경향 (배열)

### 캐시 메모리 쓰기 정책
* 즉시 쓰기: 캐시 메모리와 메모리에 동시에 쓰기
  * 장점: 캐시 메모리 데이터를 항상 최신 상태로 유지
  * 단점: 데이터 쓰기 진행시 버스의 사용 시간과 쓰기 시간이 늘어남
* 지연 쓰기: 캐시 메모리에만 값을 써 두었다가 추후 수정된 데이터를 한 번에 메모리에 반영
  * 장점: 쓰기 시간을 절약
  * 단점: 메모리와 캐시 메모리 데이터 일관성이 꺠질 수 있다.

## 보조기억장치
* 플래시 메모리 기반 저장장치
* 비휘발성
* HDD, SSD

### RAID
* 데이터의 안정성, 성능을 위해 여러개의 보조기억장치를 하나의 장치 처럼 사용하는 기술
* RAID0: 데이터를 단순하게 나누어 저장하는 방식
  * 장점: 빠른 입출력 처리
  * 단점: 디스크 문제 발생시 안전 하지 않은 데이터
* RAID1: 완전한 복사본을 만들어 저장하는 구성 방식
  * 장점: 복구가 간단하고 안전성이 높다.
  * 단점: 복사본으로 용량을 많이 사용한다.
* RAID4: 페리티 정보를 저장하는 디스크를 따로 두는 구성 방식
  * 장점: RAID1 비해 적은 하드디스크로 안전하게 데이터 보관
  * 단점: 패리티를 저장하는 장치에 병목 현상이 발생
* RAID5: 패리티 정보를 분산 저장하는 구성 방식
  * 장점: RAID4 병목 현상을 보안
  * 단점: 디스크 1개 장애만 허용
* RAID6: 서로 다른 2개의 패리티 구성 방식
  * 장점: RAID5 비해 안전성 증가
  * 단점: 새로운 정보를 저장할 떄마다 함께 저장할 패리티가 2개이므로 쓰기 속도 느림

### Nested RAID
* 여러 RAID 레벨을 혼합한 방식
  * RAID10 - RAID0과 RAID1 혼합
  * RAID50 - RAID0과 RAID5 혼홥

## 입출력장치
* 컴퓨터 외부에 연결되어 컴퓨터 내부와 정보를 교환하는 장치

### 장치 컨트롤러
  * CPU, 입출력장치 사이 통신 중계
  * 모든 입출력 장치는 각자의 장치 컨트롤러 통해 컴퓨터와 통신

### 장치 드라이버
  * 장치 컨트롤러를 동작하는 프로그램

### 프로그램 기반 입출력
  * 프로그램 명령어로 입출력 작업을 수행하는 방법
  * 고립형 입출력: 메모리 주소 공간, 입출력장치 주소 공간 분리
  * 메모리 맵 입출력: 메모리 주소 공간, 입출력장치 주소 공간 통일

### 인터럽트 기반 입출력 (다중 인터럽트)
* 여러 입출력장치로부터 동시다발적으로 발생하는 인터럽트
* 프로그래머블 인터럽트 컨트롤러(PIC)
  * PIC 여러 장치 컨트롤러에 연결되어 인터럽트 신호를 우선순위 판별하여 CPU 전달
  * PIC 계층적으로 구성하여, 많은 인터럽트 신호를 받을 수 있다.

### DMA 입출력
* DMA 컨트롤러 하드웨어 필요
* CPU 병목을 방지하고자 DMA 컨트롤러 통해 직접 메모리에 접근하는 방식
* 사이클 스틸링: DMA 제어기가 데이터를 전송할 때, CPU의 메모리 버스 사용을 일시적으로 빼앗아 사용하는 방식
  * 시스템 버스를 CPU, DMA 컨트롤러 서로 제한적으로 사용
  * DMA 컨트롤러, CPU 서로 사용을 양보하면서 사용

### PCIe
* PCI 입출력 버스의 발전된 형태
* SSD, GPU, 네트워크 인터페이스 카드 등 다양한 입출력장치 연결
* 버전에 따라서 지원 속도가 다르다.