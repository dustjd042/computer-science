# 네트워크 
* 노드, 간선으로 이루어진 그래프 형태의 데이터 구조

### 네트워크의 종류
* 망형, 트리형, 링형,성형, 버스형

### 호스트
* 네트워크의 가장자리에서 최초로 정보를 송신하고 최종 수신하는 노드

### 중간 노드
* 가장자리에 위치한 호스트가 주고받는 정보를 수신지까지 안정적으로 전송하는 역할
* 스위치, 라우터, 공유기

### LAN
* 근거리 네트워크

### WAN
* 원거리 네트워크
* 일반적으로 인터넷 서비스 업체(ISP) 구축 및 관리

### 패킷 교환 네트워크
* 패킷 단위로 주고받는 정보를 쪼개서 송수신 (수신지 재조립)
* 패킷: 송수신되는 데이터 단위
  * 페이로드: 패킷에서 송수신하고자 하는 데이터
  * 헤더, 트레일러: 패킷에 추가되는 부가 정보

### 주소
* 패킷 수신지 특정 목적 사용
* IP, MAC

### 전송 방식
* 유니캐스트: 송신지와 수신지 일대일 전송
* 브로드캐스트: 모든 호스트에게 전송
  * 브로드캐스트 도메인: 브로드캐스트 전송 범위
* 멀티캐스트: 네트워크 동일 그롭 전송
* 애니캐스트: 네트워크 동일 그롭 가까운 호스트 전송

### 네트워크 참조 모델
* 통신이 이루어지는 단계를 계층적 표현
* 계층 별로 관련 프로토콜 처리

### OSI 7 (이론적)
* 응용 계층: 네트워크 프로그램 실행되는 계층 (HTTP, DNS 등 포로토콜 존재)
* 표현 계층: 데이터 표현 목적 계층 (인코딩, 압축, 암호화)
* 세션 계층: 응용 프로그램 간의 연결 상태 관리 계층
* 전송 계층: 신뢰성 있는 전송 보장 목적 계층 (포트 정보 통해 응용 프로그램 식별)
* 네트워크 계층: 네트워크 간 통신을 위한 계층 (IP 프로토콜)
* 데이터 링크 계층: 네트워크 호스트끼리 올바르게 정보를 주도받기 위한 계층 (MAC 주소 활용)
* 물리 계층: 비트 신호 주고받는 계층

### TCP/IP 모델 (실질적)
* 응용 계층: 응용, 표현, 세션 계층 유사
* 전송 계층: 전송 계층 유사
* 인터넷 계층: 네트워크 계층 유사
* 네트워크 계층: 데이터 링크 계층 유사

### 캡슐화
* 송신 과정에서 헤더 혹은 트레일러 추가 과정

### 역캡슐화
* 수신 과정에서 헤더 혹은 트레일러 제거 과정

### PDU (프로토콜 데이터 단위)
* 응용, 표현, 세션: 메시지
* 전송: 세그먼트(TCP), 데이터그램(UDP)
* 네트워크: IP 패킷
* 데이터 링크: 프레임 
* 물리: 비트

## 물리 계층
### 이더넷 (IEEE 802.3)
* 통신 매체를 통해 신호를 송수신하는 방법
* 최근 정의된 버전의 표준의 경우 더 빠른 속도 지원

### 이더넷 프레임
* 이더넷 기반의 네트워크에서 주고받는 프레임
  * 프리앰블: 이더넷 프레임 시작 의미
  * 송수신지 MAC 주소: 송신지, 수신지 특정할 수 있는 MAC 주소 명시
  * 타입, 길이: 상위 계층 프로토콜 정보 및 데이터 길이 명시
  * 데이터: 페이로드 상위 계층 전달 혹은 전달 받을 데이터
    * MTU (1,500 Byte) 이더넷 프레임으로 전송 가능한 최대 데이터 크기
    * MTU (1,500 Byte) 보다 큰 데이터 전송의 경우 여러 패킷으로 나누어 전송
    * MTU (1,500 Byte) 네트워크 계층 패킷의 최대 크기
  * FCS: CRC 오류 검출용 값 명시

### 네트워크 인터페이스 (NIC)
* 노드와 네트워크 연결하는 통로
* MAC(물리) 주소 부여
* 호스트 이해하는 혹은 통신 매체 신호 변환
* 입출력장치와 동일하게 작동

### 허브
* 물리 계층 장비
* 여러 대의 호스트를 연결하는 장치
* 전달받은 신호를 모든 포트 전송
* 반이중 모드 통신 지원
* 콜리전 도메인: 반이중 통신으로 인해 데이터 충돌 발생 영역

### L2 스위치
* 데이터 링크 장비
* MAC(물리) 주소를 통해 전달 받은 신호를 목적지 호스트 전송 (MAC 주소 학습 기능)
* VLAN 기능 통해 논리적인 네트워크 영역 분리
* 전이중 모드 통신 지원

## 네트워크 계층
### IP 프로토콜
* 비연결형 신뢰할 수 없는 프로토콜
* IP 주소 지정: 송신지, 수신지 IP 명시
* 단편화: MTU 단위보다 큰 패킷의 경우 여러 패킷으로 나누어 전송
  * 식별자: 특정 패킷이 어떠한 패킷에서 나누어 졌는지 파악 목적
  * 플래그: DF (단편화 금지), MP (단편화 패킷 여부) 표시
  * 단편화 오프셋: 초기 단편화 데이터로부터 상대적 위치 표시
* 경로 MTU 발견: IP 패킷을 주고 받는 경로 존재하는 네트워크 장비의 처리 가능한 MTU 크기 고려
* TTL (Time To Live)
  * 패킷이 라우터를 거칠 때마다 1씩 감소
  * TTL이 0에 도달하면 라우터는 패킷 폐기 및 ICMP Time Exceeded 메시지 송신
  * 무한 루프 방지 목적 (잘못된 라우팅 정보로 인한 루프 방지)

### IP 주소
* 32비트 구성 네트워크 주소, 호스트 주소 공간 유동적
* 클래스풀 주소 체계: 클래스를 바탕으로 IP 주소 관리하는 주소 체계
  * A, B, C 클래스: 호스트 주소 3옥텟, 2옥텟, 1옥텟 구성
  * D, E 클래스: 특수 목적 위한 예약된 클래스
* 예약된 호스트 주소 공간
  * 호스트 주소 전부 0 경우 네트워크 자체를 지칭하는 주소
  * 호스트 주소 전부 1 경우 브로드캐트를 위한 주소
* 패킷의 송수신 과정에서 IP 주소 우선 활용
* 네트워크 간 통신 IP 주소, 네트워크 내 통신 MAC 주소 활용

### IP 주소 지정
* 기본 게이트웨이: 호스트 속한 네트워크 외부 나가기 위한 경로
* DNS 주소: 도메인 네임으로 IP 주소를 질의하는 서버의 주소
* DHCP: IP 주소 동적으로 할당받고자 하는 호스트에게 IP 주소 할당 프로토콜
  * 사용 가능한 기간 존재 (임대 갱신 통해 IP 주소 유지)

### ICMP
* 신뢰할 수 없는 비 연결 통신을 보안하기 위한 목적
* 피드백 메시지 (ICMP) 통해 IP 전송 결과 참고 가능

### ARP
* IP 주소 통해 MAC 주소 파악
* ARP 요청 메시지의 경우 브로드캐스트 메시지
* ARP 테이블을 통해 IP, MAC 주소 관리

### 클래스리스 주소 체계
* 클래스 주소 체계 IP 주소 낭비를 방지하는 목적
* 서브넷 마스크 통해 네트워크, 호스트 주소 공간 분리
* CIDR 표기법 활용 (10.0.0.1/24)

### 라우터
* 서로 다른 네트워크 속한 두 호스트가 네트워크 통신 가능하도록 지원
* 라우팅: 최적 경로 결졍하고 해당 경로 통해 패킷 전송 과정

### NAT
* SNAT (Source NAT): 송신 패킷의 출발지 주소를 변환하여 내부 네트워크의 여러 호스트가 하나의 공인 IP로 인터넷 연결
* DNAT (Destination NAT): 수신 패킷의 목적지 주소를 변환하여 외부에서 들어오는 트래픽을 내부 호스트로 

## 전송 계층
* 포트를 통해 프로세스를 식별
* 송신지 포트, 수신지 포트 헤더 명시

### UDP
* 신뢰할 수 없는 통신, 비 연결형 통신
* 헤더: 송신지 포트, 수신지 포트, 길이, 체크섬

### TCP
* 신뢰할 수 있는 통신, 연결형 통신
* 헤더: 송신지 포트, 수신지 포트, 길이, 체크섬
  * 순서 번호 필드: TCP 패킷의 올바른 송수신 순서 보장을 위해 매겨진 번호
  * 확인 응답 번호 필드: 수신하길 기대하는 순서번호 (순서 번호 필드 + 1 전달)
  * 제어 비트 (플래그 비트)
    * ACK: 세그먼트 승인 위한 비트
    * SYN: 연결 수립 위한 비트
    * FIN: 연결 종료 위한 비트

### TCP 연결 수립 (쓰리 웨이 핸드셰이크)
* 엑티브 오픈: 처음 연결 요청 시작하는 과정
* 패시브 오픈: 연결 요청을 수신 뒤 과정
* 연결 수립 과정
  * A ---> B: SYN 세그먼트, A순서 번호 필드 전송
  * A <--- B: SYN, ACK 세그먼트, B순서 번호, A순서 번호 + 1 확인 응답 필드 전송
  * A ---> B: ACK 세그먼트, A순서 번호, B순서 번호 + 1 확인 응답 필드 전송

### TCP 연결 종료 수립 (포 웨이 핸드셰이크)
* 엑티브 클로즈: 처음 연결 종료 요청 시작하는 과정
* 패시브 클로즈: 연결 종료 요청을 수신 뒤 과정
* 연결 종료 과정
  * A ---> B: FIN 세그먼트
  * A <--- B: ACK 세그먼트
  * A <--- B: FIN 세그먼트
  * A ---> B: ACK 세그먼트

### TCP 오류, 흐름, 혼잡 제어
* 오류 제어: 중복된 ACK 세그먼트, 타임아웃 발생의 경우 잘못된 세그먼트 존재 확인 및 재전송
* 흐름 제어: 송신 호스트는 수신 호스트에 데이터 처리 양을 고려해 데이터 송신
  * 수신 윈도우 필드: 수신 호스트 수신 가능 크기 명시
* 혼잡 제어: 송신 호스트가 트래픽으로 인해 패킷의 처리 속도 고려 송수신 데이터 조절
  * 혼잡 윈도우 필드: 혼잡 없이 전송 가능한 데이터 양 명시
  * 혼잡 제어 알고리즘 (AIMD): 혼잡 감지 안되는 경우 (데이터양 + 1), 혼잡 감지 되는 경우 (데이터양 / 2)

### TCP 상태 관리
* 비연결 상태
  * CLOSED: 연결 없는 상태
  * LISTEN: 연결 대기 상태
* 연결 수립 과정
  * SYN-SENT: SYN 세그먼트 전송 뒤 대기 상태
  * SYN-RECEIVED: SYN + ACK 세그먼트 전송 뒤 대기 상태
  * ESTABLISHED: 연결 수립 상태
* 연결 종료 과정
  * FIN-WAIT-1: 엑티브 클로즈 호스트 FIN 세그먼트 통해 연결 종료 요청
  * CLOSE-WAIT: FIN 세그먼트를 받은 패시브 클로스 ACK 세그먼트 전송
  * FIN-WAIT-2: FIN-WAIT-1 상태에서 ACK 세그먼트 받은 상태
  * LAST-ACK: CLOSE-WAIT 상태에서 FIN 세그먼트 전송 뒤에 대기 상태
  * TIME-WAIT: 액티브 클로즈 호스트 ACK 세그먼트 전송한 뒤 접어드는 상태

## 응용 계층
### 도메인 네임 리졸빙 과정
* 로컬 네임 서버 도메인 네임 질의
* 루트 네임 서버 -> 하위 레벨 도메인 서버로 계층적 질의

### DNS 캐시
* 질의 과정이 반복 되는 경우 네트워크 트래픽 증가
* 네임 서버들은 기존 응답 결과를 임시 저장

### URI
* 자원을 식별하기 위한 정보
* URN: 이름 자원을 식별하는 방식
* URL: 위치로 자원을 식별하는 방식
  * scheme: 자원 접근 방식 명시 (프로토콜)
  * authority: 호스트 특정할 수 있는 IP 주소 혹은 도메인 네임
  * path: 자원 위치 상세 경로

### HTTP
* 요청과 응답 기반 프로토콜
* 미디어 독립적 프로토콜
* 스테이트리스 프로토콜
* 지속 연결 프로토콜

### HTTP 버전별 특징
* HTTP 1.0: 모든 요청에 대해서 TCP 쓰리 웨이 핸드셰이크 과정 존재
* HTTP 1.1: TCP 기반 동작하며, 지속 연결 기술 (keep-alive) 지원
* HTTP 2.0: 바이너리 데이터 기반 송수신, 헤더 압축, 서버 푸시, 멀티플레싱
* HTTP 3.0: UDP 기반 QUIC 프로토콜 기반 동작

### 쿠키
* HTTP 스테이트리스 특성 보안 대표적 수단
* 서버 생성되어 클라이언트 측 저장되는 이름, 값 형태의 데이터
* HTTP 헤더 `Set-Cookie: 이름=값;`

### 세션
* 서버가 클라이언트 상태를 유지하기 위해 사용하는 저장 공간으로, 로그인 정보나 사용자 데이터를 서버 측에 보관한다.
* 클라이언트는 세션 ID만 쿠키 등을 통해 전달하며, 서버는 이를 기반으로 사용자를 식별한다.
* HTTP의 무상태(Stateless) 특성을 보완하기 위해 사용된다.

### JWT (JSON Web Token)
* 사용자 정보를 JSON 형태로 담아 서명한 토큰으로, 서버가 별도의 세션 저장 없이 사용자를 인증할 수 있다.
* 클라이언트는 JWT를 저장한 뒤 요청마다 함께 전달하며, 서버는 토큰의 서명을 검증해 사용자 정보를 확인한다.
* 주로 인증(Authentication)과 인가(Authorization)에 사용되며, Stateless한 구조를 가진다.

### 캐시
* 응답 받은 자원의 사본을 임시 저장하여 불필요한 대역폭 및 시간 낭비 방지
* 날짜 기반 원본 자원 변경 여부 질의 `If-Modified-Since`
* 엔티티 태그 기반 원본 자원 변경 여부 질의 `If-Non-Match`

### 콘텐츠 협상
* 같은 자원에 대해 여러 표현 중 클라이언트에 가장 적합한 자원 표현을 제공하는 기술

### TLS (Transport Layer Security)
* 통신 과정에서 누군가 패킷을 보는 것을 방지 (암호화)
* 통신 데이터 변조 방지 (무결성)
* 통신 상대에 대한 인증 (인증)
* TLS 메시지 송수신 단계
  * TCP 쓰리 웨이 핸드셰이크
  * TLS 핸드셰이크
  * 메시지 송수신

### TLS 핸드셰이크 과정
* Client Hello: TLS 버전 사용 가능한 암호화 알고리즘, 해시 함수 전달 (클라이언트 -> 서버)
* Server Hello: 선택된 TLS 버전 및 암호화 알고리즘, 해시 함수 전달 (서버 -> 클라이언트)
* Certificate: 인증 기관을 통해 발급 받은 인증서를 통해 서버 입증 (서버 -> 클라이언트)
* Message: 메시지 인증 코드(MAC) 및 해시 함수를 통해 매시지 암호화 (서버 <-> 클라이언트)

### 웹소켓
* 양방향 통신
* 실시간 네트워크
* 여러개 프레임 모여 메시지 구성
* 프레임 경우 헤더, 페이로드 구성

### 웹소켓 단점
* 웹소켓 경우 HTML5 이후 표준 스팩에 추가
* 문자열만 주고 받을 수 있다.
* 별도 형식이 정해져 있지 않기 때문에 어플리케이션에서 해석이 어렵다.
* 서브 프로토콜 (STOMP) 통해 별도에 메시지 형태 정의

### STOMP
* 채팅 통신을 위한 형식 정의
* Pub/Sub(발행/구독) 구조
* 약간의 오버헤드 메시지마다 명령어와 헤더 정보 존재

### 웹소켓 연결 수립 과정
* HTTP, HTTPS 활용 핸드셰이크
* 클라이언트 웹소켓 연결 요청
  * GET 
  * HTTP/1.1 
  * Upgrade: websocket 
  * Connection: Upgrade
  * Sec-WebSocket-Key: 랜덤 생성값 Base64 인코딩
* 서버 웹소켓 연결 응답
  * 101 Switching Protocols
  * Upgrade: websocket
  * Connection: Upgrade
  * Sec-WebSocket-Accept: (Sec-WebSocket-Key + GUID) SHA-1 해시 계산, Base64 인코딩
* WS, WSS 전환
* Close frame 통해 연결 종료

### 웹소켓 재연결 방식
* 웹소켓 연결 종료 상태 코드 활용
* 웹소켓 라이브러리 재연결 기능 활용
* 특정 상황에 맞게 직접 재연결 처리