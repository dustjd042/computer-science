# 데이터페이스
* 원하는 기능 동작을 위해 마땅히 저장해야 하는 정보 집합

### DBMS
* 데이터베이스 관리 목적 프로그램
  * 관계형: MySQL, Oracle
  * NoSQL: Redis, Mongo

### 파일에 저장하는 경우 단점
* 데이터 일관성 및 무결성 유지 불가
* 중복 데이터 발생
* 데이터 변경 시 연관 데이터 변경 어려움
* 정교한 검색 어려움
* 백업 및 복구 어려움

### 데이터 베이스 용어
* 엔티티: 독립적으로 존재 가능한 객체
* 속성: 엔티티 특성
* 엔티티 집합: 같은 속성을 공유하는 엔티티
* 릴레이션: 이차원 테이블 형태의 엔티티 집합
* 레코드: 데이터베이스 저장된 각각 엔티티
* 필드: 데이터베이스 저장된 엔티티 속성
* 스키마: 레코드 구조와 제약 조건
* 트랜잭션: 데이터베이스와 논리적 상호작용 단위
* Cardinality (튜플): 행의 총 갯수
* Degree (차수): 열의 총 갯수

### ACID
* 원자성: 하나의 트랜젹션 결과는 모두 성공 혹은 모두 실패
* 일관성: 트랜잭션 전후로 데이터베이스 일관된 상태(데이터베이스 지켜야하는 규칙) 유지
* 격리성: 여러 트랜잭션이 서로 간섭하지 않도록 보장하는 성질 (레이스 컨디션 방지)
* 지속성: 트랜잭션 성공적인 완료된 후에 해당 결과과 영구적 반영되는 성질

### 키
* 후보키: 유일성과 최소성을 무두 만족하는 키
* 복합키: 두 필드 이상으로 구성된 후보키
* 슈퍼키: 유일성 만족하며 최소성은 만족하지 않는 키
* 기본키: 여러 후보키 중에서 대표로 선택된 키
* 대체키: 기본키가 아닌 후보키
* 외래키: 다른 테이블 기본키 참조하는 필드 테이블 간의 참조 관계 형성 키

### 무결성 제약 조건
* 도메인 제약 조건: 필드 타입 및 제약, 필드의 경우 원자값 구성 조건
* 키 제약 조건: 키로 지정된 필드에 중보값 존재 불가 조건
* 엔티티 무결성 제약 조건: 키본키로 지정된 값은 고유하며, NULL 존재 불가 조건
* 참조 무결성 제약 조건: 외래키를 통해 다른 테이블 참조하는 경우 데이터 일관성 지키는 제약 조건

### 참조 테이블 삭제 / 수정 경우
* 연산 제한
* 기본값 설정
* NUll 설정
* 연쇄 변경

### SQL
* DDL: 데이터 정의
* DML: 데이터 조작
* DCL: 데이터 제어
* TCL: 트랜젝션 제어

### 뷰
* 참조한 기본 테이블을 통해 만들어진 논리적 가상 테이블
  * 독립성: 기본 테이블 구조가 변경 되는 경우에도 영향이 없다.
  * 유연성: 복잡한 SQL 미리 정의하여 쉽게 접근
  * 자동반영: 기본 테이블 변경시 뷰 데이터 실시간 변경

### 인덱스
* 책에 있는 색인처럼 테이블의 특정 컬럼 데이터를 정렬해 두어 검색 속도를 획기적으로 높여주는 기술
* 인덱스 관리를 위한 추가 저장 공간이 필요하며, 데이터의 삽입, 수정, 삭제 명령 수행시 오히려 성능이 저하

### 데이터베이스 이상현상
* 이상 현상은 데이터의 중복 때문에 발생 이러한 문제를 해결하기 위해 테이블을 관련 있는 속성들로만 쪼개는 정규화 과정이 필요
  * 삽입 이상: 새로운 데이터를 삽입할 때, 원하지 않는 불필요한 정보까지 함께 삽입해야 하거나 불필요한 정보 없이는 데이터를 삽입할 수 없는 현상
  * 삭제 이상: 테이블에서 특정 데이터를 삭제할 때, 삭제해서는 안 되는 유용한 정보까지 연쇄적으로 함께 삭제되는 현상
  * 갱신 이상: 테이블의 특정 데이터를 수정할 때, 중복된 데이터 중 일부만 수정되어 데이터의 불일치 일어나는 현상

### 정규화
* 잠재적인 문제가 발생하지 않도록 테이블 필드 구성
* 제1정규형: 모든 속성은 원자값
* 제2정규형: 기본키 아닌 모든 필드는 기본키 완전히 종속
* 제3정규형: 기본키 아닌 모든 필드는 기본키 이행적 종속 없는 상태
* 보이스 코드 정규형: 후보키 아닌 결정자 제거
* 제4정규형: 테이블에 속한 여러 개의 독립적인 다치 종속성 분리
* 제5정규형: 조인 종속 제거 테이블을 더 이상 분해할 수 없을 때까지 완전히 분리
